spring:
  application:
    name: token-gateway
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: http://hadoop102:8848
    gateway:
      routes:
        - id: account
          uri: lb://token-account
          order: 1
          predicates:
            - Path=/account/**
