# record 类 ConfigurationProperties 绑定演示配置
demo:
  user:
    username: |- # 没有最后的换行符
      jim,
      tom
    password: |  # 每个都有换行符
      123
      456
    dog: # 必须缩进两格
      min: 1 # 必须缩进四格
      max: 100
