<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.axinger.sys.mapper.SysPersonMapper">

    <resultMap id="BaseResultMap" type="com.github.axinger.sys.domain.SysPersonEntity">
        <id property="id" column="id"/>
        <result property="age" column="age"/>
        <result property="birthday" column="birthday"/>
        <result property="createTime" column="create_time"/>
        <result property="deleted" column="deleted"/>
        <result property="gender" column="gender"/>
        <result property="name" column="name"/>
        <result property="updateTime" column="update_time"/>
        <result property="version" column="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,age,birthday,
        create_time,deleted,gender,
        name,update_time,
        version
    </sql>


    <select id="customSqlSegment" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="ew != null and ew.SqlSelect != null and ew.SqlSelect != ''">
                ${ew.SqlSelect}
            </when>
            <otherwise>
                <include refid="Base_Column_List"/>
            </otherwise>
        </choose>
        FROM
        sys_person
        ${ew.customSqlSegment}
    </select>

    <select id="sqlSegment" resultMap="BaseResultMap">
        SELECT
        <choose>
            <when test="ew != null and ew.SqlSelect != null and ew.SqlSelect != ''">
                ${ew.SqlSelect}
            </when>
            <otherwise>
                <include refid="Base_Column_List"/>
            </otherwise>
        </choose>
        FROM
        sys_person
        where deleted == 0
        <if test="ew != null">
            <if test="ew.nonEmptyOfWhere">
                AND
            </if>
            ${ew.sqlSegment}
        </if>

    </select>


    <select id="findMaxIdList" resultMap="BaseResultMap">

        SELECT *
        FROM `device_status_timeline` bb
                 INNER JOIN (SELECT MAX(`id`) AS mid, `device_id` AS m_device_id
                             FROM `device_status_timeline` ${ew.customSqlSegment}
                             GROUP BY `device_id`) AS tb ON bb.`id` = tb.mid

    </select>
</mapper>
